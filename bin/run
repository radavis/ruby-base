#!/usr/bin/env bash

set -Eeuxo pipefail

opts="$*"
image="ruby-base"

# docker run [OPTIONS] IMAGE [COMMAND] [ARG...]
docker run \
  --rm \
  --interactive \
  --tty \
  --volume "$(pwd):/home/app" \
  ${image} \
  ${opts}
